# 部署环境
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lee_blog?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username:
    password:
  # redis配置
  redis:
    host: localhost
    port: 6379
    password:
  # rabbitmq配置
  rabbitmq:
    host: localhost
    port: 5672
    username:
    password:
